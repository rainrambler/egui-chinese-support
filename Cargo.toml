[package]
name = "egui-chinese-support"
version = "0.1.2"
edition = "2024"
authors = ["rainrambler <wanganyu@outlook.com>"]
description = "Cross-platform Chinese font loading for egui applications"
license = "MIT"
repository = "https://github.com/rainrambler/egui-chinese-support"
homepage = "https://github.com/rainrambler/egui-chinese-support"
readme = "README.md"
keywords = ["egui", "chinese", "font", "gui", "cjk"]
categories = ["gui", "internationalization", "text-processing"]
rust-version = "1.85"
exclude = ["target/", "examples/target/"]

[dependencies]
egui = "0.35"

[dev-dependencies]
eframe = "0.35"

# Optional platform-specific dependencies for advanced font discovery
[target.'cfg(windows)'.dependencies]
winapi = { version = "0.3", features = ["winuser", "wingdi", "fileapi", "handleapi", "processenv"], optional = true }

[target.'cfg(target_os = "macos")'.dependencies]
core-text = { version = "21.0.0", optional = true }

[target.'cfg(target_os = "linux")'.dependencies]
fontconfig = { version = "0.10", optional = true }

[features]
default = []
platform-fonts = ["winapi", "core-text", "fontconfig"]
